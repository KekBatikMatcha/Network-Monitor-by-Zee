# Z • Net Monitor Dashboard

Z • Net Monitor is a lightweight **network uptime and latency monitoring system** designed for **authorized networks only**.  
It uses a Python monitoring agent to check host availability and a PHP + JavaScript dashboard to display live results in a clean, professional UI.

This project demonstrates practical skills in **network monitoring, system integration, and web dashboards**.

---

## ✨ Features

- Manual target monitoring (explicit allow-list)
- Real-time status detection:
  - **UP**
  - **DEGRADED**
  - **DOWN**
- Latency measurement (milliseconds)
- Failure counters with thresholds
- Recent status-change alerts
- Search, sort, and refresh controls
- JSON-based data exchange
- Clean, responsive dashboard UI

---

## 🧱 Tech Stack

- **Python** — monitoring engine
- **PHP** — API layer (reads JSON data)
- **HTML & CSS** — dashboard layout and styling
- **JavaScript** — live data fetching and rendering
- **JSON / JSONL** — status snapshots, history, alerts



